# Dialyzer warning filters. Keep each entry scoped as tightly as possible so real
# warnings in our own code still fail the gate. Empty for now — the router has real
# routes (U4), so the earlier empty-router false positive no longer occurs.
[]
